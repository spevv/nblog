extends ../layout
block content
    h1 Login
    if(message.length > 0)
        = message

    form(action="/login", method="post")
        label Email
        input(type="text" class="form-control" name="email", value="test@test.com")
        br
        label Password
        input(type="password" class="form-control" name="password", value="1111")
        br
        button(type="submit" class="btn btn-warning btn-lg") Login

    hr
    <p>Need an account? <a href="/signup">Signup</a></p>
    <p>Or go <a href="/">home</a>.</p>
